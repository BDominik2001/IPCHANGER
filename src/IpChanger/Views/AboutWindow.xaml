<Window x:Class="IpChanger.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource L.About.Title}"
        Height="440" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/app.ico"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Segoe UI">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Logó + név -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,4,0,0">
            <Image Source="/Assets/logo.png" Width="96" Height="96"
                   RenderOptions.BitmapScalingMode="HighQuality" />
            <TextBlock Text="{Binding AppName}" Style="{StaticResource HeaderText}"
                       FontSize="22" HorizontalAlignment="Center" Margin="0,12,0,0" />
            <TextBlock Text="{DynamicResource L.About.Subtitle}" Style="{StaticResource CaptionText}"
                       HorizontalAlignment="Center" TextAlignment="Center"
                       TextWrapping="Wrap" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Adatok -->
        <Border Grid.Row="1" Style="{StaticResource Card}" VerticalAlignment="Center" Margin="0,18,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource L.About.VersionLabel}"
                           Style="{StaticResource CaptionText}" Margin="0,0,16,8" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Version}" Margin="0,0,0,8" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource L.About.CreatorLabel}"
                           Style="{StaticResource CaptionText}" Margin="0,0,16,0" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Creator}" />
            </Grid>
        </Border>

        <!-- Gombok -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="{DynamicResource L.About.OpenGitHub}" Style="{StaticResource AccentButton}"
                    Command="{Binding OpenGitHubCommand}" MinWidth="150" Margin="0,0,10,0" />
            <Button Content="{DynamicResource L.About.Close}" IsCancel="True"
                    MinWidth="100" Click="OnClose" />
        </StackPanel>
    </Grid>
</Window>
